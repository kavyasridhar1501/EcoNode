# Test-only dependencies.
#
# Prophet and supabase are intentionally NOT listed here: tests/conftest.py
# stubs both out, since the unit test suite only exercises pure functions
# (data validation, carbon math, green-window selection, analysis stats) and
# never needs a real Prophet model or a real database connection. This keeps
# the test job fast on every push instead of compiling Prophet's Stan backend.
# Install requirements.txt as well for a full pipeline run.
pandas>=2.1.0
numpy>=1.24.0
requests>=2.31.0
pytest>=7.4.0
